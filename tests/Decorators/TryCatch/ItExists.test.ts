import {TryCatch} from "../../../src/Decorators/TryCatch";

describe("TryCatch.ts", () => {
    test("It exists", () => {
        // Arrange
        // Act
        // Assert
        expect(TryCatch).not.toBeNull();
        expect(TryCatch).not.toBeUndefined();
    });
});
